sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t duke-count/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true duke-count/docker-react npm run test -- --coverage
